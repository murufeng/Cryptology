#include <string>
#include <iostream>
using namespace std;
int main()
{
	int i,k;
	char m[10000];
	cout<<"please input simple as m:";
	gets(m);
	cout<<"密文为"<<m<<endl;
	cout<<"请输入解密密钥:";
	cin>>k;
	cout<<"解密密钥为:"<<k<<endl;
	for(i=0;i<strlen(m);i++)
	{
		if(m[i]<='z'&&m[i]>='a')
		{
			cout<<(char)((m[i]-'a'+26-k)%26+'a');
		}
		else if(m[i]<='Z'&&m[i]>='A')
		{
			cout<<(char)((m[i]-'A'+26-k)%26+'A');
		}
		else if(m[i]<=9&&m[i]>=0) 
		{
			cout<<((m[i]+26-k)%26);
		}
		else
			cout<<m[i];
	}
	cout<<endl;
	 return 0;
}
